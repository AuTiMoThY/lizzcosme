/**
 * >> lizzUI.init()
 */

init: function () {
    const _ = this;

    _.goTop();
    _.headerSearch();
    _.navToggle();


    // _.mmenu().init();

    // console.log(mqDown.sm.matches);
    // function windowResize() {
    //     console.log(mqDown.sm.matches);
    // }

    // window.onresize = windowResize;
},